<Window x:Class="DesktopApp.StudentForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopApp"
        mc:Ignorable="d"
        Title="StudentForm" Height="450" Width="800">

  <Window.Resources>
    <ResourceDictionary>
      <local:Converter x:Key="myConv" />
    </ResourceDictionary>
  </Window.Resources>
  <Grid>
    <StackPanel>
      <DockPanel Margin="10">
        <Label Content="FirstName"></Label>
        <TextBox Text="{Binding FirstName, Converter={StaticResource myConv}}"></TextBox>
      </DockPanel>

      <DockPanel Margin="10">
        <Label Content="LastName"></Label>
        <TextBox Text="{Binding LastName, Converter={StaticResource myConv}}"></TextBox>
      </DockPanel>

      <Button Content="Save" Margin="10" Click="SaveStudent"></Button>
    </StackPanel>
     
  </Grid>
</Window>
